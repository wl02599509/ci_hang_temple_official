<turbo-frame id="cancel_modal_frame">
  <%= render "cancel_modal",
        activity: @activity,
        registration_ids: @registration_ids,
        has_paid: @has_paid,
        has_mixed: @has_mixed %>
</turbo-frame>
